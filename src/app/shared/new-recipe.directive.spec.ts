import { NewRecipeDirective } from './new-recipe.directive';

describe('NewRecipeDirective', () => {
  it('should create an instance', () => {
    const directive = new NewRecipeDirective();
    expect(directive).toBeTruthy();
  });
});
